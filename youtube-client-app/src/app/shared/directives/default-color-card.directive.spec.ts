import { DefaultColorCardDirective } from './default-color-card.directive';

describe('DefaultColorCardDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultColorCardDirective();
    expect(directive).toBeTruthy();
  });
});
