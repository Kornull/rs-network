import { CustomButtonStyleDirective } from './custom-button-style.directive';

describe('CustomButtonStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomButtonStyleDirective();
    expect(directive).toBeTruthy();
  });
});
